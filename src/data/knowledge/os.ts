import type { ModuleKnowledge } from '../types'

export const osKnowledge: ModuleKnowledge = {
  moduleId: 'os',
  chapters: [
    {
      id: 'os-c1',
      title: '进程管理',
      hot: 3,
      content: '## 核心概念\n\n**进程三态模型及转换**：\n- 就绪 → 运行：进程被调度**获得 CPU**\n- 运行 → 就绪：**时间片用完**或被更高优先级进程**抢占**\n- 运行 → 阻塞：等待 I/O、P 操作资源不足等，是进程的**主动**行为\n- 阻塞 → 就绪：所等待的**事件发生**（如 I/O 完成）被**被动唤醒**；阻塞态不能直接转为运行态\n\n五态模型增加**新建（创建）态**与**终止态**；引入挂起后分为活动态与静止（挂起）态，激活与挂起只作用于就绪、阻塞两态。\n\n## PV 操作（信号量机制，必考）\n\n- **P 操作（wait，申请资源）**：S = S - 1；若 S < 0，进程**阻塞**并进入该信号量的等待队列\n- **V 操作（signal，释放资源）**：S = S + 1；若 S <= 0，**唤醒**等待队列中的一个进程\n- S < 0 时 |S| 表示被阻塞的进程数；S 的初值表示初始可用资源数\n\n**互斥与同步的使用规则**：\n- **互斥**（临界资源互斥访问）：P(mutex) 与 V(mutex) 出现在**同一个进程**中，紧夹临界区，mutex 初值为 1\n- **同步**（控制先后顺序）：P、V 操作出现在**不同进程**中，规则是**前操作之后 V，后操作之前 P**\n\n**生产者-消费者模型**（缓冲区容量 n）：信号量 mutex=1（互斥）、empty=n（空闲缓冲区）、full=0（产品数）：\n- 生产者：P(empty) → P(mutex) → 放产品 → V(mutex) → V(full)\n- 消费者：P(full) → P(mutex) → 取产品 → V(mutex) → V(empty)\n- **先做同步 P 再做互斥 P（两个 P 顺序不能颠倒，否则可能死锁）；两个 V 的顺序可以颠倒**\n\n## 易混淆对比\n\n- 运行→阻塞是主动请求（等 I/O）；阻塞→就绪是被动唤醒；就绪只缺 CPU，阻塞缺资源（事件）。\n- 互斥的 P、V 在同一进程内成对出现；同步的 P、V 跨进程配对。\n\n## 记忆口诀\n\n> P 减一，小于零则睡；V 加一，小于等于零则唤；\n> 同步 P 在前，互斥 P 在后；生产者先 P 空再 P 锁。',
    },
    {
      id: 'os-c2',
      title: '死锁',
      hot: 3,
      content: '## 核心概念\n\n**死锁产生的四个必要条件**（必须同时满足，缺一不可）：\n1. **互斥条件**：资源一次只能被一个进程占有\n2. **请求保持条件**（占有且等待）：进程占有部分资源又申请新资源\n3. **不剥夺条件**（不可抢占）：已分配资源不能被强行夺走\n4. **环路条件**（循环等待）：存在进程-资源的循环等待链\n\n**死锁处理策略**：\n- **预防**：静态破坏四个条件之一（如一次性申请全部资源破坏请求保持；资源有序分配破坏环路等待）\n- **避免**：动态分配前判断系统安全性，代表是**银行家算法**\n- **检测与解除**：利用资源分配图化简（死锁定理），通过资源剥夺、进程撤销、进程回退解除死锁\n\n## 高频计算：资源数量的安全下限\n\n**n 个进程，每个进程最多需要 m 个资源，系统不死锁至少需要 n × (m - 1) + 1 个资源。**\n\n原理：最坏情况下每个进程都先获得 m-1 个资源（共 n(m-1) 个），再多 1 个就能保证某进程凑齐 m 个资源运行完毕并释放资源。\n\n例：5 个进程各最多需 3 个资源，至少 5×(3-1)+1 = 11 个资源可保证不会发生死锁。\n\n## 银行家算法思想\n\n进程申请资源时**先试探性分配**，再检查系统是否处于**安全状态**（能否找到一个安全序列，使每个进程都能依次获得全部所需资源、运行完毕并释放资源）；若能找到安全序列则正式分配，否则让申请进程等待。核心：**分配后仍安全才分配**。\n\n## 易混淆对比\n\n- 预防是事前静态破坏条件；避免是分配时动态检测（银行家算法）。\n- 安全状态必定无死锁；不安全状态**不一定**死锁，但存在死锁可能。\n- 有环路不一定死锁（资源可共享时）；资源独占时死锁必含环路。\n\n## 记忆口诀\n\n> 互斥、请求保持、不剥夺、循环等待，四条件缺一不可；\n> 至少资源数：n 乘 m 减一再加一；银行家：安全才放款。',
    },
    {
      id: 'os-c3',
      title: '存储管理',
      hot: 3,
      content: '## 核心概念\n\n**页式存储管理**：逻辑地址 = **页号 + 页内偏移**；物理地址 = **物理块号（页框号）+ 页内偏移**，页内偏移**不发生变化**。\n\n**地址变换步骤**（必考）：\n1. 由页面大小确定偏移位数（页 4KB → 偏移 12 位），将逻辑地址分解为页号与页内偏移\n2. 用页号查**页表**（由页表基址寄存器定位），得到对应物理块号；页号越界则产生越界中断\n3. 物理块号拼接页内偏移即得物理地址；访问**快表 TLB** 命中可减少一次访存\n\n**段式管理**：按程序逻辑分段，地址为二维（段号+段内偏移），便于共享与保护，产生**外碎片**；**段页式**：先分段、段内分页（段号+页号+页内偏移），兼顾两者优点。\n\n## 页面置换算法（必考对比）\n\n| 算法 | 淘汰规则 | 特点 |\n| --- | --- | --- |\n| OPT 最佳置换 | 淘汰**未来最长时间不再被访问**的页 | 缺页率最低，理论最优但无法实现，用于评价其他算法 |\n| FIFO 先进先出 | 淘汰**最先进入内存**的页 | 实现简单；可能出现 **Belady 异常**（分配页框增多、缺页次数反而增多） |\n| LRU 最近最少使用 | 淘汰**最近最久未被访问**的页 | 性能接近 OPT，硬件开销较大，无 Belady 异常 |\n\n注意：缺页中断属于**内中断（故障）**；访问页面时无论是否缺页都要先查页表；发生缺页才引起页面调入与置换。\n\n## 易混淆对比\n\n- **内碎片**：分配单位内部浪费的空间（页式存在页内碎片）；**外碎片**：分区之间的小空隙（可变分区、段式存在）。\n- FIFO 按**进入内存的时间**淘汰，LRU 按**最近访问的时间**淘汰；Belady 异常只有 FIFO 会出现。\n- 分页是一维地址、对程序员透明；分段是二维地址、对用户可见。\n\n## 记忆口诀\n\n> OPT 看未来，FIFO 看资历，LRU 看人气；\n> 页号查页表，块号拼偏移，偏移永不变。',
    },
    {
      id: 'os-c4',
      title: '文件与设备管理',
      hot: 2,
      content: '## 核心概念\n\n**文件索引结构计算**（重点）：设每个盘块可存放 256 个盘块地址，盘块大小按题设（如 1KB）：\n- 直接地址项：直接指向数据盘块，i 个直接项可寻址 i 块\n- 一级间接索引项：可寻址 **256** 块；二级间接：256 × 256 = 65536 块；三级间接：256^3 块\n- **最大文件长度 = (直接块数 + 256 + 256^2 + 256^3) × 盘块大小**\n\n**位示图法**（空闲空间管理）：用一位表示一个盘块的使用情况（1 占用、0 空闲），所需字数 = 盘块总数 ÷ 每字位数。做题注意字号、位号与盘块号的换算公式，以及**编号从 0 还是从 1 开始**。\n\n**磁盘调度算法**：\n\n| 算法 | 规则 | 特点 |\n| --- | --- | --- |\n| FCFS 先来先服务 | 按请求到达先后服务 | 公平，平均寻道距离长 |\n| SSTF 最短寻道优先 | 每次选距当前磁头**最近**的请求 | 性能较好，但远处请求可能饥饿 |\n| SCAN 电梯调度 | 沿一个方向扫到**端点**后折返 | 对两端请求有利 |\n| C-SCAN 单向扫描 | 到端点后直接返回起点再单向扫描 | 各磁道等待时间更均匀 |\n\n**设备管理要点**：按使用方式分独占设备、共享设备、**虚拟设备**（SPOOLING 假脱机技术用磁盘模拟独占设备）；磁盘属**直接（随机）存取**设备，磁带属**顺序存取**设备；DMA、通道、缓冲技术用于提高 I/O 效率。\n\n## 易混淆对比\n\n- SCAN 扫到磁盘端点才折返；LOOK 变体扫到该方向**最后一个请求**即折返。\n- SSTF 只看寻道距离、可能导致饥饿；C-SCAN 返回途中不服务请求。\n\n## 记忆口诀\n\n> 一索二百五十六，二索二百五十六平方，三索立方再乘块大小；\n> SCAN 像电梯，扫到头再折返。',
    },
  ],
}
