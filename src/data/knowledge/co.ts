import type { ModuleKnowledge } from '../types'

export const coKnowledge: ModuleKnowledge = {
  moduleId: 'co',
  chapters: [
    {
      id: 'co-c1',
      title: '数据的表示与运算',
      hot: 3,
      content: '## 核心概念\n\n**进制转换**：二进制转十进制**按权展开**，如 1011.1B = 8+2+1+0.5 = 11.5；十进制转二进制：整数部分**除 2 取余、逆序排列**，小数部分**乘 2 取整、顺序排列**。\n\n**机器数编码**（符号位 0 正 1 负）：\n\n| 编码 | 正数 | 负数 | 8 位表示范围 | 0 的表示 |\n| --- | --- | --- | --- | --- |\n| 原码 | 符号位 0，数值同真值 | 符号位 1，数值同真值 | -127 ~ +127 | 有 +0 和 -0 两种 |\n| 反码 | 同原码 | 符号位不变，数值位按位取反 | -127 ~ +127 | 两种 |\n| 补码 | 同原码 | 反码加 1 | **-128 ~ +127** | **唯一** |\n| 移码 | 补码符号位取反 | 补码符号位取反 | -128 ~ +127 | 唯一 |\n\n补码可将减法统一为加法，是运算基础；移码用于浮点数**阶码**，便于比较大小。\n\n**浮点数**：N = 尾数 × 基数^阶码。**阶码决定表示范围，尾数决定表示精度**。规格化要求尾数最高数值位为有效位（基数 2 时 0.5 ≤ |尾数| < 1）；尾数左移阶码减 1 称左规，尾数右移阶码加 1 称右规。\n\n## 考点清单\n\n| 考点 | 要点 |\n| --- | --- |\n| 奇偶校验 | 增加 1 位校验位使 1 的个数为奇/偶数；只能发现奇数个位出错，**不能纠错** |\n| CRC 循环冗余校验 | 数据后补 0（生成多项式最高次幂个 0），对生成多项式做**模 2 除法**（异或运算），余数即 CRC 码；检错能力强、硬件易实现 |\n| 海明码 | 可**纠正 1 位错**；校验位位数 r 满足 **2^r ≥ n + r + 1**（n 为信息位）；校验位放在 1、2、4、8 等 2^i 位置 |\n\n## 易混淆对比\n\n- 原码与反码中 0 有两种表示，补码与移码中 0 唯一；8 位补码比原码多表示 -128。\n- 奇偶校验只能检错不能纠错；海明码既能检错又能纠 1 位错。\n- 浮点数阶码位数越多范围越大，尾数位数越多精度越高，两者位数此消彼长。\n\n## 记忆口诀\n\n> 负数补码：取反加一；海明位数：2 的 r 次不小于 n 加 r 加 1；\n> 阶码管范围，尾数管精度。',
    },
    {
      id: 'co-c2',
      title: '存储体系',
      hot: 3,
      content: '## 核心概念\n\n**存储层次结构**：寄存器 → Cache → 主存（内存）→ 辅存（磁盘）→ 后备存储（磁带/光盘）。自上而下：速度越来越**慢**、容量越来越**大**、单位价格越来越**低**。Cache-主存层次由**硬件**实现，解决**速度**匹配问题；主存-辅存层次由**操作系统**管理（虚拟存储器），解决**容量**问题。\n\n## 考点清单\n\n**Cache 三种映射方式**：\n\n| 方式 | 映射规则 | 主存地址划分 | 特点 |\n| --- | --- | --- | --- |\n| 直接映射 | 主存块只能装入固定行（行号 = 块号 mod Cache 行数） | 区号 + 行号 + 块内地址 | 硬件简单、冲突率高 |\n| 全相联映射 | 可装入任意一行 | 块号 + 块内地址 | 最灵活、比较电路成本高 |\n| 组相联映射 | 组间直接映射、组内全相联 | 区号 + 组号 + 块内地址 | 折中方案，实际最常用 |\n\n**命中率与平均访问时间计算**（必考）：\n- 命中率 h = Nc / (Nc + Nm)，Nc 为命中 Cache 的次数，Nm 为访问主存的次数\n- **平均访问时间 = h × tc + (1-h) × tm**（tc 为 Cache 存取周期，tm 为主存存取周期）\n\n**磁盘存储器**：\n- **存取时间 = 寻道时间 + 旋转延迟 + 传输时间**；旋转延迟通常取平均值即**半圈时间**。例：10000 转/分 → 每转 6ms → 平均旋转延迟 3ms\n- 磁盘容量按磁头数 × 柱面数 × 每道扇区数 × 每扇区字节数计算（格式化容量）\n\n## 易混淆对比\n\n- Cache 对**程序员完全透明**（硬件自动管理）；虚拟存储器对应用程序员透明，但需要 OS 与硬件配合。\n- 直接映射每个主存块只有一个固定位置；全相联任意放；组相联先按直接方式定组、再在组内任意放。\n\n## 记忆口诀\n\n> 平均访问时间 = 命中率乘 Cache 周期 + 未命中率乘主存周期；\n> 磁盘耗时：寻道加半圈再加传输。',
    },
    {
      id: 'co-c3',
      title: '指令系统与流水线',
      hot: 3,
      content: '## 核心概念\n\n**寻址方式**（指令如何寻找操作数）：\n\n| 寻址方式 | 有效地址 EA | 特点 |\n| --- | --- | --- |\n| 立即寻址 | 操作数直接在指令中 | 取指即取数，速度最快 |\n| 直接寻址 | EA = 地址码 | 简单，寻址范围受地址码位数限制 |\n| 间接寻址 | EA = (地址码) 所指单元的内容 | 需多次访存，速度慢 |\n| 寄存器寻址 | 操作数在寄存器中 | 不访存，速度快 |\n| 寄存器间接寻址 | EA = 寄存器内容 | 一次访存 |\n| 变址寻址 | EA = 形式地址 + 变址寄存器 | 适合处理数组与循环程序 |\n| 基址寻址 | EA = 形式地址 + 基址寄存器 | 面向系统，用于程序重定位、扩大寻址范围 |\n| 相对寻址 | EA = PC + 偏移量 | 用于转移指令，便于程序浮动 |\n\n**CISC 与 RISC 对比**：\n\n| 对比项 | CISC 复杂指令集 | RISC 精简指令集 |\n| --- | --- | --- |\n| 指令数量与长度 | 指令多、长度可变 | 指令少、长度固定 |\n| 控制方式 | 微程序控制 | 硬布线（组合逻辑）控制 |\n| 访存指令 | 多数指令可访存 | 只有 LOAD/STORE 指令访存 |\n| 执行方式 | 指令串行执行 | 流水线技术 |\n| 通用寄存器 | 较少 | 大量通用寄存器 |\n| 典型代表 | x86 | ARM、MIPS、RISC-V |\n\n## 流水线计算（每年必考）\n\n设流水线分为 k 段，各段时间不等：\n- **流水线周期 Δt = 最长段的时间**\n- n 条指令总时间 **T = (k + n - 1) × Δt**\n- **吞吐率 TP = n / [(k+n-1)Δt]**；最大吞吐率 = 1/Δt\n- **加速比 S = nk / (k+n-1)**\n- 效率 E = n / (k+n-1)\n\n## 易混淆对比\n\n- 流水线周期取决于**最长段**，不是各段平均值。\n- 超标量：配置多条流水线并行；超流水：阶段再细分、主频更高；单条流水线公式不变。\n\n## 记忆口诀\n\n> 周期取最长，总时 K 加 N 减一；\n> 吞吐 N 除总时间，加速 NK 比上 K 加 N 减一。',
    },
    {
      id: 'co-c4',
      title: '总线与可靠性',
      hot: 2,
      content: '## 核心概念\n\n**总线（BUS）分类**：\n- **数据总线 DB**：**双向**，传送数据，其位数（宽度）决定一次能传送的数据位数\n- **地址总线 AB**：**单向**（CPU 发出），位数 n 决定直接寻址空间为 **2^n**\n- **控制总线 CB**：传送控制信号与状态信号（读/写、中断请求、总线请求等）\n\n按层次还可分为：芯片内总线、系统总线（ISA、PCI、PCIe）、通信总线（RS-232、USB、SCSI）。\n\n## 可靠性计算（重点）\n\n**串联系统**（任一部件失效则系统失效）：**R = R1 × R2 × … × Rn**。串联部件越多可靠性越低；失效率近似相加。\n\n**并联系统**（全部部件失效系统才失效，冗余备份）：**R = 1 - (1-R1) × (1-R2) × … × (1-Rn)**。并联越多可靠性越高。\n\n**N 模冗余系统**：N 个相同模块 + 表决器，少数服从多数输出。若各模块可靠度均为 R0，系统可容忍 m 个模块失效，则 R = Σ C(N,i) × R0^i × (1-R0)^(N-i)，i 从 m+1 取到 N。经典 **3 模冗余（容 1 错）：R = R0^3 + 3 × R0^2 × (1-R0)**。\n\n**串并混合系统**：先将局部串联或并联化简，再代入整体公式。平均无故障时间 MTBF = 1/λ（λ 为失效率）。\n\n## 易混淆对比\n\n- 串联直接相乘可靠度；并联先求各失效概率之积、再用 1 减，方向不能弄反。\n- 2m+1 个模块的表决系统可容忍 m 个模块失效。\n- 地址总线单向、数据总线双向，方向判断是常考细节。\n\n## 记忆口诀\n\n> 串联乘可靠，并联补失效；\n> 表决系统用组合数，从 m+1 加到 N。',
    },
    {
      id: 'co-c5',
      title: '体系结构分类与性能评价',
      hot: 2,
      content: '## 核心概念\n\n**Flynn 体系结构分类**（按指令流 IS 与数据流 DS 的多倍性，必考）：\n\n| 类型 | 指令流 | 数据流 | 典型结构 |\n| --- | --- | --- | --- |\n| SISD 单指令流单数据流 | 单 | 单 | 传统单处理器、冯·诺依曼机 |\n| SIMD 单指令流多数据流 | 单 | 多 | 阵列处理机、向量处理机、GPU |\n| MISD 多指令流单数据流 | 多 | 单 | 实际上不存在（仅理论） |\n| MIMD 多指令流多数据流 | 多 | 多 | 对称多处理机 SMP、多核、集群 |\n\n按存储器组织又分为：冯·诺依曼结构（指令与数据存于同一存储器，取指取数串行形成瓶颈）与哈佛结构（指令与数据分开存储、可并行访问）。\n\n## 考点清单：性能评价\n\n- 基本指标：主频、字长、CPI（执行一条指令的平均时钟周期数）、内存容量\n- **MIPS = 主频(Hz) / (CPI × 10^6)**（每秒百万条指令数）；MFLOPS 按浮点运算次数统计\n- **响应时间**：从提交请求到完成的时间；**吞吐量**：单位时间完成的任务数\n- 基准程序测试（Benchmark）：用标准测试程序集（如 SPEC）比较机器性能\n\n**加速比计算（重点）**：某部件改进后系统加速比 **S = 1 / [(1-Fe) + Fe/Se]**，Fe 为可改进部分占原总执行时间的比例，Se 为该部件的加速倍数。若给出改进前后总时间，直接用：加速比 = 改进前时间 / 改进后时间。\n\n例：某部件耗时占 60%（Fe=0.6），提速 5 倍，则 S = 1/(0.4+0.6/5) = 1/0.52 ≈ 1.92。\n\n## 易混淆对比\n\n- SIMD 是一条指令控制多个数据并行（阵列机）；MIMD 是多个指令流独立处理各自数据流（多处理机）。\n- MIPS 只宜比较相同指令集的机器，跨体系结构比较应使用基准程序。\n\n## 记忆口诀\n\n> 加速比分母：一减 Fe 加 Fe 比 Se；\n> 单指多数 SIMD，多指多数 MIMD。',
    },
  ],
}
