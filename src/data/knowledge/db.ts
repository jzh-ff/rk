import type { ModuleKnowledge } from '../types'

export const dbKnowledge: ModuleKnowledge = {
  moduleId: 'db',
  chapters: [
    {
      id: 'db-c1',
      title: '体系结构与 ER 模型',
      hot: 3,
      content: '## 核心概念\n\n**数据库三级模式结构**：\n- **外模式**（子模式/用户视图）：对应**视图**与部分基本表，描述个别用户看到的数据；一个数据库可有多个外模式\n- **概念模式**（模式/逻辑模式）：对应**基本表**，描述全体数据的全局逻辑结构；只有一个\n- **内模式**（存储模式/物理模式）：对应**存储文件**，描述数据的物理存储结构与存取方式；只有一个\n\n**两级映像与数据独立性**（必考对应关系）：\n- **外模式/模式映像** → 保证**逻辑独立性**：模式改变时只需修改该映像，外模式与应用程序不变\n- **模式/内模式映像** → 保证**物理独立性**：存储结构改变时模式不变\n\n## ER 模型转关系模式规则（必考）\n\n- 一个**实体**转换为一个关系模式，实体的属性即关系的属性，实体的码即关系的码\n- **1:1 联系**：可转换为独立关系模式，也可与**任意一端**实体合并（在合并端加入另一端的主键及联系属性）\n- **1:n 联系**：可转换为独立关系模式，也可与 **n 端**实体合并（在 n 端加入 1 端的主键）\n- **m:n 联系**：**必须转换为独立的关系模式**，属性为两端实体的主键加联系自身属性，主键为**两端主键的组合**\n- 三个及以上实体间的多元联系：转换为独立关系模式，主键为各实体主键的组合\n\nER 图要素：**矩形=实体型、椭圆=属性、菱形=联系**，线段上标注 1、n、m。\n\n## 易混淆对比\n\n- 逻辑独立性对应外模式/模式映像，物理独立性对应模式/内模式映像，考试常交叉设错，务必记牢。\n- 视图是虚表（不实际存数据），对应外模式；基本表对应模式；存储文件对应内模式。\n\n## 记忆口诀\n\n> 外-模映像管逻辑，模-内映像管物理；\n> 一一并任意，一对多并多方，多对多必独立，主键取双方。',
    },
    {
      id: 'db-c2',
      title: '关系代数与 SQL',
      hot: 3,
      content: '## 核心概念\n\n**关系代数五种基本运算**：**并 ∪、差 -、笛卡尔积 ×、选择 σ、投影 π**。选择按条件挑**行**（水平方向），投影按属性挑**列**（垂直方向，自动去重）。\n\n**扩展运算**：交 ∩、连接（θ 连接、等值连接）、**自然连接**（按同名列等值连接并**去掉重复列**）、外连接、**除法 ÷**。\n\n**除法的意义**（重点）：R÷S 的结果为 R 中那些 X 取值，其象集**包含 S 在 Y 上的全部取值**，适合解答含**全部、所有**语义的查询。例：查询选修了全部课程的学生 = 选课(学号,课程号) ÷ 课程(课程号)。\n\n## SQL 高频考点\n\n- 连接查询：FROM A a JOIN B b ON a.id = b.aid，用别名简化书写\n- **分组统计**：GROUP BY 分组列 HAVING 组条件；**WHERE 过滤行（不能使用聚合函数），HAVING 过滤组（可使用聚合函数）**；分组后 SELECT 子句只能出现分组列与聚合函数（COUNT、SUM、AVG、MAX、MIN）\n- 嵌套查询：IN、比较运算符、EXISTS（相关子查询）；含全部语义可用双重 NOT EXISTS（相当于除法）\n- 权限控制：**GRANT** 权限 ON 对象 TO 用户 [WITH GRANT OPTION 允许转授]；**REVOKE** 收回权限\n- 视图：CREATE VIEW 视图名 AS SELECT 语句\n- 语句逻辑执行顺序：FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY\n\n## 易混淆对比\n\n- 等值连接保留重复列，自然连接去掉重复属性列。\n- 选择 σ 选行、投影 π 选列，方向相反；投影可能因去重减少元组个数。\n- DELETE 只删除表中元组、保留表结构；DROP 删除整个表对象。\n\n## 记忆口诀\n\n> 五基本：并、差、笛卡尔积、选择、投影；\n> 行走 WHERE，组走 HAVING；全部语义找除法。',
    },
    {
      id: 'db-c3',
      title: '规范化理论',
      hot: 3,
      content: '## 核心概念\n\n**函数依赖**：X → Y 表示 X 唯一确定 Y。\n- **部分函数依赖**：候选键的真子集即可决定非主属性。例：(学号,课程号)→姓名，而学号单独即可决定姓名\n- **传递函数依赖**：X→Y，Y→Z，且 Y 不是候选键、Y 不能决定 X，则 Z 传递依赖于 X\n\n**候选键求法**（必考步骤）：\n1. 只出现在函数依赖**左部**的属性：必包含在**每个候选键**中（作为起点）\n2. 只出现在**右部**的属性：必不在任何候选键中（直接排除）\n3. 左右都出现或都未出现的属性：与起点组合逐一试探，能推出全部属性的最小组合即候选键\n\n**主属性**=属于某个候选键的属性；**非主属性**=不属于任何候选键的属性。\n\n## 范式判断（必考）\n\n| 范式 | 判断条件 |\n| --- | --- |\n| 1NF | 所有属性均为**不可再分**的原子值 |\n| 2NF | 1NF 基础上，消除非主属性对候选键的**部分**函数依赖 |\n| 3NF | 2NF 基础上，消除非主属性对候选键的**传递**函数依赖 |\n| BCNF | 每个非平凡函数依赖 X→Y 的决定因素 X 都**包含候选键**（是超键） |\n\n包含关系：1NF ⊇ 2NF ⊇ 3NF ⊇ BCNF，规范化程度依次升高。\n\n**模式分解**：判断两个性质——**无损连接性**（分解后自然连接可恢复原关系，用表格法判断）与**保持函数依赖**（分解后各子模式依赖集的并集逻辑蕴涵原依赖集）。可达 3NF 的分解既能无损又保持依赖；**BCNF 分解可保证无损，但不一定保持函数依赖**。\n\n## 易混淆对比\n\n- 2NF 针对**部分**依赖，3NF 针对**传递**依赖；判断前必须先求出候选键与主属性。\n- 3NF 允许主属性对码的部分/传递依赖；BCNF 要求所有依赖的决定因素都含码。\n\n## 记忆口诀\n\n> 消部分进二，消传递进三，决定因素含码进 BC；\n> 只在左必入选，只在右必排除。',
    },
    {
      id: 'db-c4',
      title: '并发控制与恢复',
      hot: 2,
      content: '## 核心概念\n\n**事务的 ACID 特性**（必考）：\n- **原子性**（Atomicity）：事务是不可分割的整体，要么全做要么全不做\n- **一致性**（Consistency）：事务执行前后数据库都保持一致状态\n- **隔离性**（Isolation）：并发执行的事务相互隔离、互不干扰\n- **持久性**（Durability）：事务提交后对数据的修改永久有效\n\n**封锁与相容矩阵**：\n- **X 锁（写锁/排他锁）**：事务对数据加 X 锁后，其他事务不能再加任何锁\n- **S 锁（读锁/共享锁）**：加 S 锁后其他事务可继续加 S 锁，但不能加 X 锁\n\n| 已持有 | 申请 S 锁 | 申请 X 锁 |\n| --- | --- | --- |\n| S 锁 | 相容 | 不相容 |\n| X 锁 | 不相容 | 不相容 |\n\n**封锁协议**：一级封锁协议（写前加 X 锁、事务结束释放）→ 防止**丢失修改**；二级（读前加 S 锁、读完即放）→ 再防**读脏数据**；三级（读前加 S 锁、事务结束才放）→ 再防**不可重复读**。\n\n**两段锁协议 2PL**：事务分为**扩展阶段（只加锁不释放）**与**收缩阶段（只解锁不加锁）**。所有事务均遵守 2PL 时，并发调度必为**可串行化**调度；但 2PL **不能防止死锁**。\n\n## 数据库恢复\n\n**冗余数据是恢复的基础**：数据转储（静态/动态转储，海量/增量转储）与日志文件。\n**日志先写原则（Write Ahead Log）**：必须**先写日志、后写数据库**；日志完整记录事务的开始、提交/回滚及每次修改的前后镜像。\n故障恢复：事务故障→**UNDO（撤销）**未完成事务；系统故障→重启后对未提交事务 UNDO、对已提交未写入磁盘的事务 **REDO（重做）**；介质故障→装入后备副本并用日志重做。\n\n## 易混淆对比\n\n- UNDO 撤销**未提交**事务；REDO 重做**已提交**但尚未落盘的事务。\n- 2PL 保证可串行化但不避免死锁；一次封锁法可避免死锁但降低并发度。\n\n## 记忆口诀\n\n> 读 S 写 X，S 遇 S 兼容、其余全冲突；\n> 两段锁：先只加锁后只解锁；先写日志后写库。',
    },
  ],
}
