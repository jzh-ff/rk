import type { Question } from '../types';

// 数据库系统模块题库（软考中级软件设计师），共 16 题：db-001 ~ db-016

export const dbQuestions: Question[] = [
  {
    id: 'db-001',
    moduleId: 'db',
    chapter: '三级模式与数据独立性',
    stem: '数据库系统采用三级模式结构。当数据库的**存储结构（内模式）发生改变**时，只需修改模式/内模式映像，可使模式和外模式保持不变，从而应用程序不必修改。这体现了数据的（）。',
    options: ['逻辑独立性', '物理独立性', '参照完整性', '位置透明性'],
    answer: 1,
    explanation: '三级模式：**外模式**（用户级视图）、**模式**（全局逻辑结构）、**内模式**（物理存储结构）；两级映像：**外模式/模式映像**保证**逻辑数据独立性**（模式改变时外模式与应用不变），**模式/内模式映像**保证**物理数据独立性**（内模式改变时模式不变）。\n\n本题改变的是存储结构（内模式），对应**物理独立性**。\n\n易错点：见到逻辑结构改变、应用不变选逻辑独立性，见到存储结构改变选物理独立性，不要混淆两级映像的方向。\n\n口诀：**物理对内（模式/内模式映像），逻辑对外（外模式/模式映像）**。',
    difficulty: 1,
    hot: true,
  },
  {
    id: 'db-002',
    moduleId: 'db',
    chapter: 'ER 模型',
    stem: '将 ER 模型转换为关系模型时，一个 **m : n 联系**应转换为（）。',
    options: ['与任意一端实体合并成一个关系模式', '与两端实体合并成一个关系模式', '一个独立的关系模式，其属性为两端实体的码加上联系自身的属性', '不需要转换，直接舍去'],
    answer: 2,
    explanation: 'ER 图转关系模式的规则：**1:1 联系**可与任意一端实体合并；**1:n 联系**并入 n 端实体，即在 n 端关系模式中加入 1 端实体的码；**m:n 联系必须单独建立一个关系模式**，其属性为两端实体的码（组合构成主键）再加上联系自身的属性。\n\n例如学生与课程之间的选修是 m:n 联系，须单独建立 sc(学号, 课程号, 成绩)，主键为 (学号, 课程号)。\n\n口诀：**1:1 随便并，1:n 并到 n 端，m:n 独立成表**。',
    difficulty: 2,
  },
  {
    id: 'db-003',
    moduleId: 'db',
    chapter: '关系代数',
    stem: '设有关系 R(A, B, C)，其元组为：(a1, b1, 5)、(a2, b2, 10)、(a1, b3, 15)。则选择运算 σ_{C>5}(R) 的结果元组数与投影运算 π_{A}(R) 的结果元组数分别为（）。',
    options: ['2 和 2', '2 和 3', '3 和 2', '3 和 3'],
    answer: 0,
    explanation: '**选择 σ 是横向操作**，按条件筛选元组：满足 C>5 的只有 (a2, b2, 10) 和 (a1, b3, 15)，元组 (a1, b1, 5) 中 5 不大于 5 被去掉，结果为 **2** 个元组。\n\n**投影 π 是纵向操作**，取指定列并**自动去掉重复元组**：A 列的值为 a1、a2、a1，去重后只剩 a1、a2，结果为 **2** 个元组。\n\n易错点：投影结果中重复的元组必须删除，忘记去重会误选 3。\n\n口诀：**选择挑行，投影挑列且去重**。',
    difficulty: 2,
  },
  {
    id: 'db-004',
    moduleId: 'db',
    chapter: '关系代数',
    stem: '关系代数中，要查询选修了**全部**课程的学生学号，最合适的运算符是（）。',
    options: ['自然连接 (⋈)', '除法 (÷)', '广义笛卡尔积 (×)', '并 (∪)'],
    answer: 1,
    explanation: '**除法运算 R ÷ S** 专门表达**全部、所有**语义：设 R(学号, 课程号) 为选课关系，S(课程号) 为全部课程，则 R ÷ S 的结果是那些在 R 中与 S 里**每一个**课程号都有对应关系的学号，即选修了全部课程的学生。\n\n除法可由 π、−、× 等基本运算导出。其余选项：自然连接按公共属性相等合并两表；× 做全组合；∪ 要求两关系同构并合并元组，都不能表达全部量词。\n\n延伸：该类查询也可用**元组关系演算**（以元组为变量的谓词表达式）或 SQL 中的 NOT EXISTS 双层否定实现。\n\n口诀：**见到全部，就用除法**。',
    difficulty: 2,
  },
  {
    id: 'db-005',
    moduleId: 'db',
    chapter: 'SQL 语言',
    stem: '在 SQL 中，条件 **WHERE grade BETWEEN 70 AND 80** 等价于（）。',
    options: ['grade >= 70 AND grade <= 80', 'grade > 70 AND grade < 80', 'grade >= 70 OR grade <= 80', 'grade IN (70, 80)'],
    answer: 0,
    explanation: 'BETWEEN a AND b 表示**闭区间 [a, b]**，包含两端点，等价于 **grade >= 70 AND grade <= 80**。\n\n易错点：选项 B 是开区间 (70, 80)，漏掉了 70 和 80 两个端点；选项 C 用 OR 连接后，条件对几乎所有成绩都成立（几乎恒真）；选项 D 只匹配 70 和 80 两个离散值。\n\n口诀：**BETWEEN 是闭区间，端点算在内**。',
    difficulty: 1,
    hot: true,
  },
  {
    id: 'db-006',
    moduleId: 'db',
    chapter: 'SQL 语言',
    stem: '在 sc(sno, cno, grade) 表中查询**平均成绩大于 80** 的学生学号及平均成绩，正确的 SQL 语句是（）。',
    options: ['SELECT sno, AVG(grade) FROM sc WHERE AVG(grade) > 80 GROUP BY sno', 'SELECT sno, AVG(grade) FROM sc GROUP BY sno HAVING AVG(grade) > 80', 'SELECT sno, AVG(grade) FROM sc GROUP BY sno WHERE AVG(grade) > 80', 'SELECT sno, AVG(grade) FROM sc HAVING AVG(grade) > 80'],
    answer: 1,
    explanation: 'WHERE 作用于**行**、在分组之前执行，其中**不能使用聚合函数**；对分组结果的筛选（含聚合函数的条件）必须用 **HAVING** 子句，且语法上位于 GROUP BY 之后。\n\n选项 A 错在 WHERE 中出现聚合函数；选项 C 错在语法顺序（WHERE 必须在 GROUP BY 之前）；选项 D 缺少 GROUP BY，整个表被聚合成一组，无法得到每个学生的平均成绩。\n\n口诀：**WHERE 先选行，GROUP BY 再分组，HAVING 后筛组；聚合条件只能进 HAVING**。',
    difficulty: 2,
  },
  {
    id: 'db-007',
    moduleId: 'db',
    chapter: 'SQL 语言',
    stem: '关于 SQL 嵌套查询中 EXISTS 与 IN 的区别，下列说法正确的是（）。',
    options: ['EXISTS 的子查询必须返回单列单值才能使用', 'NOT IN 的子查询结果中含有 NULL 时，NOT IN 条件可能永远不为真，而 EXISTS 只判断元组是否存在，不受 NULL 影响', 'IN 与 EXISTS 语义完全相同，任何时候都可以无条件互换', 'EXISTS 是非相关子查询，IN 是相关子查询'],
    answer: 1,
    explanation: '**EXISTS (子查询)** 只判断子查询结果**是否存在元组**（返回真或假），不关心具体列值，因此不受 NULL 影响，通常配合相关子查询使用；NOT EXISTS 还可通过双重否定表达选修了**全部**课程这类查询。\n\n**NOT IN** 遇到子查询结果含 NULL 时，例如 x NOT IN (1, NULL) 恒不为 TRUE，会导致查询结果为空，这是高频易错点。\n\n其余选项均错误：EXISTS 不要求返回单值；IN 与 EXISTS 并非无条件等价（NULL 语义不同）；EXISTS 通常用于相关子查询而非非相关子查询。\n\n口诀：**EXISTS 看有无，NOT IN 怕 NULL**。',
    difficulty: 2,
  },
  {
    id: 'db-008',
    moduleId: 'db',
    chapter: 'SQL 语言',
    stem: '要将表 sc 上的 SELECT 和 INSERT 权限授予用户 user1，并**允许 user1 将该权限再授予其他用户**，正确的 SQL 语句是（）。',
    options: ['GRANT SELECT, INSERT ON sc TO user1 WITH GRANT OPTION', 'GRANT SELECT, INSERT ON sc TO user1', 'GRANT SELECT, INSERT TO user1 ON sc', 'REVOKE SELECT, INSERT ON sc FROM user1'],
    answer: 0,
    explanation: '授权语句语法：**GRANT 权限 ON 对象 TO 用户 [WITH GRANT OPTION]**。\n\n是否允许转授由 **WITH GRANT OPTION** 决定：加上它，user1 才能把获得的权限再授予其他用户；不加则只能自己使用。选项 C 权限与对象位置颠倒，语法错误；选项 D 是收回权限的 REVOKE 语句，语义相反。\n\n口诀：**权限 ON 表 TO 用户，要转授就加 WITH GRANT OPTION**。',
    difficulty: 1,
  },
  {
    id: 'db-009',
    moduleId: 'db',
    chapter: 'SQL 语言',
    stem: '关于数据库视图（VIEW），下列说法**错误**的是（）。',
    options: ['视图是虚表，数据库中只存放它的定义而不存放对应数据', '视图能简化用户操作，并对数据提供一定的安全保护', '任何视图都可以直接进行插入和更新操作', '定义视图时加 WITH CHECK OPTION 可保证通过视图更新的数据仍满足视图的查询条件'],
    answer: 2,
    explanation: '视图是从基本表（或其他视图）导出的**虚表**，数据库中只存储其定义，数据仍存放在基本表中。\n\n视图的作用：简化用户查询、提供一定的**逻辑数据独立性**、配合授权机制实现**安全性**（对用户隐藏部分行和列）。\n\n**并非所有视图都可更新**：由单个表导出、包含主键、无聚合的行列子集视图通常可以更新；含 GROUP BY、聚合函数、DISTINCT、多表连接的视图一般不能直接更新。选项 C 说法过于绝对，是错误项。\n\nWITH CHECK OPTION 用于保证通过视图执行 INSERT/UPDATE 后的行仍满足视图定义中的条件。\n\n口诀：**视图存定义不存数据；带聚合和分组的视图不可更新**。',
    difficulty: 2,
  },
  {
    id: 'db-010',
    moduleId: 'db',
    chapter: '键与完整性约束',
    stem: '设关系 R(A, B, C, D) 的函数依赖集 F = {A→B, B→C, C→D, D→A}，则 R 的候选键为（）。',
    options: ['仅 AB', 'A、B、C、D 都是候选键', '仅 A', '仅 AD'],
    answer: 1,
    explanation: '候选键求解步骤：①只在函数依赖**左部**出现的属性必在候选键中；②只在**右部**出现的属性必不在候选键中；③两侧都出现或都不出现的属性需借助**属性闭包**验证。本题 A、B、C、D 均两侧出现，逐个求闭包。\n\n**A+ = {A, B, C, D}**：由 A→B 得 B，由 B→C 得 C，由 C→D 得 D，能推出全部属性，故 A 是候选键。\n\n同理 **B+ = {B, C, D, A}**（B→C→D→A）、**C+ = {C, D, A, B}**、**D+ = {D, A, B, C}** 均为全集，因此 A、B、C、D **都是候选键**。\n\n易错点：候选键不一定唯一，也不一定是复合属性；四个属性构成闭环时，从任意一个出发都能推出全部属性。\n\n口诀：**左部必进键，右部必出局，中间算闭包，闭包满全集就是键**。',
    difficulty: 3,
    hot: true,
  },
  {
    id: 'db-011',
    moduleId: 'db',
    chapter: '键与完整性约束',
    stem: '关系 sc(sno, cno, grade) 中，sno 是引用 student(sno) 的外键。若向 sc 插入一个元组，其 sno 的值在 student 表中不存在，则该操作违反了（）。',
    options: ['实体完整性', '参照完整性', '用户定义完整性', '不会违反任何完整性约束'],
    answer: 1,
    explanation: '**实体完整性**：主键属性不能取空值且主键值唯一；**参照完整性**：外键的值或者为空，或者必须等于被参照关系中某个元组的主键值；**用户定义完整性**：针对具体应用的业务约束（如 0 ≤ grade ≤ 100）。\n\n本题 sc.sno 是外键，其值在被参照表 student 的主键中不存在，违反**参照完整性**。\n\n口诀：**主键管实体（非空唯一），外键管参照（为空或存在），业务规则用户定**。',
    difficulty: 1,
  },
  {
    id: 'db-012',
    moduleId: 'db',
    chapter: '函数依赖与规范化',
    stem: '设关系模式 R(学号, 姓名, 系号, 系主任)，函数依赖集 F = {学号→姓名, 学号→系号, 系号→系主任}。则 R 最高满足第几范式，且存在非主属性对码的何种函数依赖？（）',
    options: ['1NF；部分函数依赖', '2NF；传递函数依赖', '3NF；部分函数依赖', 'BCNF；无异常依赖'],
    answer: 1,
    explanation: '候选键为**学号**（单属性码），非主属性为姓名、系号、系主任。\n\n由于码是单属性，姓名、系号、系主任都**完全函数依赖**于码，不存在部分函数依赖，故 R 至少满足 **2NF**。\n\n但存在 学号→系号、系号→系主任（系号不是码、系主任是非主属性），即**系主任传递函数依赖于学号**，传递依赖未消除，故 R 不满足 3NF，最高为 **2NF**。\n\n升级方法：将 R 分解为 R1(学号, 姓名, 系号) 和 R2(系号, 系主任)，即可消除传递依赖达到 3NF。BCNF 进一步要求所有非平凡函数依赖的决定因素都是候选键。\n\n口诀：**属性不可再分 1NF，消除部分依赖 2NF，消除传递依赖 3NF，决定因素全是键 BCNF**。',
    difficulty: 3,
    hot: true,
  },
  {
    id: 'db-013',
    moduleId: 'db',
    chapter: '函数依赖与规范化',
    stem: '关于关系模式分解的性质，下列说法正确的是（）。',
    options: ['无损连接分解要求分解后的各关系模式做自然连接后能恢复原关系的全部元组，且不产生原关系中不存在的元组', '保持函数依赖的分解一定也是无损连接的分解', '无损连接的分解一定也保持函数依赖', '将一个关系模式分解为两个关系模式时，该分解必然是无损的'],
    answer: 0,
    explanation: '**无损连接分解**：对分解后的各关系模式做自然连接，结果与原关系**完全相等**，既不丢失元组也不产生多余的元组。**保持函数依赖分解**：分解后各模式上函数依赖集的并集能逻辑蕴含原依赖集。\n\n两个性质**互不蕴含**，所以选项 B、C 都是错误的。\n\n选项 D 错误：将 R 分解为 {R1, R2} 无损的**充要条件**是 (R1∩R2) → (R1−R2) 或 (R1∩R2) → (R2−R1) 在 F 中成立，并非任何二分分解都无损。\n\n口诀：**连接还原不多不少叫无损；二分无损看交集能否决定差集**。',
    difficulty: 2,
  },
  {
    id: 'db-014',
    moduleId: 'db',
    chapter: '并发控制',
    stem: '事务 T1 已对数据项 A 加了 S 锁且尚未释放，此时事务 T2 申请对 A 加 X 锁，则 T2 将（）。',
    options: ['立即获得 X 锁并与 T1 并发执行', '等待，直到 T1 释放其 S 锁', '抢占 T1 的 S 锁并升级为 X 锁', '迫使 T1 立即回滚'],
    answer: 1,
    explanation: '封锁相容矩阵：**S 锁与 S 锁相容**（多事务可同时共享读），**S 与 X、X 与 S、X 与 X 均不相容**。T1 持有 S 锁时 T2 申请 X 锁，两类锁冲突，T2 只能**进入等待队列**，直到 T1 释放 S 锁。\n\n延伸考点：**两段锁协议（2PL）**要求事务分为扩展（只加锁不解锁）和收缩（只解锁不加锁）两个阶段，遵循 2PL 的调度一定是**可串行化**的，但 2PL **不能预防死锁**；预防死锁可用一次封锁法（一次申请全部锁）或顺序封锁法。\n\n判断可串行化调度的常用方法是**冲突可串行化**：通过交换不冲突操作的次序，若能将调度调整为某个串行调度，则其是可串行化的。\n\n口诀：**S 加 S 可以，见到 X 就等；两段锁保串行，不保无死锁**。',
    difficulty: 2,
    hot: true,
  },
  {
    id: 'db-015',
    moduleId: 'db',
    chapter: '数据库恢复',
    stem: '数据库系统在运行中发生故障后重启。对于**已提交、但其修改尚未写入磁盘**的事务，恢复子系统应执行的操作是（）。',
    options: ['UNDO（撤销）', 'REDO（重做）', 'ROLLBACK（回滚）', '建立检查点（CHECKPOINT）'],
    answer: 1,
    explanation: '恢复的依据是**日志文件**，并遵循**日志先写（WAL）原则**：\n\n- **已提交**事务的修改若尚未写入磁盘，则根据日志**重做（REDO）**该事务的全部修改，保证事务的**持久性（Durability）**；\n- **未提交（未完成）**事务的修改若已写入磁盘，则执行 **UNDO（撤销）**将其回滚，保证事务的**原子性（Atomicity）**。\n\n检查点（CHECKPOINT）定期将日志缓冲和数据缓冲写回磁盘，恢复时只需扫描最近检查点之后的日志，从而缩短恢复时间；转储（备份）是后备恢复手段。ACID 还包括一致性（Consistency）与隔离性（Isolation）。\n\n口诀：**已提交做 REDO，未提交做 UNDO**。',
    difficulty: 2,
  },
  {
    id: 'db-016',
    moduleId: 'db',
    chapter: '分布式与 NoSQL',
    stem: '关于分布式数据库与 NoSQL 数据库，下列说法**错误**的是（）。',
    options: ['分布式数据库通过数据分片（水平分片、垂直分片）和多副本提高系统的可用性与查询效率', 'CAP 理论指出一致性（C）、可用性（A）、分区容忍性（P）三者不能同时完全满足', 'NoSQL 数据库完全支持 ACID 事务和复杂的多表连接查询', 'Redis、MongoDB、HBase 都属于 NoSQL 数据库'],
    answer: 2,
    explanation: '选项 A、B、D 均为正确说法。\n\n选项 C 错误：NoSQL 数据库通常**放弃固定关系模式与强事务（ACID）**，换取良好的水平扩展能力和高性能，一般遵循 **BASE** 原则（基本可用、软状态、最终一致性），不支持或弱化复杂的多表连接；强事务与复杂查询正是关系数据库的优势。\n\nCAP 三者不可兼得，分布式环境下分区容忍性 P 是必选项，工程上通常在 C 与 A 之间权衡（如偏 CP 的 HBase、偏 AP 的 Cassandra）。\n\n口诀：**关系库讲 ACID，NoSQL 讲 BASE；CAP 三选二，P 是必选项**。',
    difficulty: 1,
  },
];
